# DEV service user → network policy attachments.
# Keys must match service user Snowflake names (with _DEV suffix).
# Policy key must exist in common/network_policies.yaml → network_policies.
#
# Example:
#   SERVICE_USER_TRANSFORM_DEV: office
#   SERVICE_USER_INGESTION_DEV: office
#
# Leave empty by default — add entries when policies are verified.
service_user_network_policies: {}
